# MLflow 서버 URI
# 로컬 서버: mlflow ui --backend-store-uri mlflow.db
MLFLOW_TRACKING_URI=http://localhost:5000

# 사용할 모델 pkl 파일 경로 (FastAPI 서버 및 CLI에서 참조)
MODEL_PATH=models/best_model.pkl

# 학습 데이터 경로
DATA_PATH=data/synthetic/hospital_data.csv
